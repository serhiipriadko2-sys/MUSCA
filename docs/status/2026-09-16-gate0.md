# MUSCA — GATE 0 verified receipt

Observed: 2026-09-16.
Status: `PASS / COMMITTED / PUSHED / CI-INVOKED / CI-VERIFIED`.

## Applied change

GATE 0 was committed on branch `docs/musca-foundations` as:

`428096325dc03bad9770266ae7328cc40f6f52d1`

Commit message: `chore: establish gate 0 CI and status`.

The commit changed only:

- `.github/workflows/ci.yml`;
- `docs/status/CURRENT.md`;
- `docs/status/2026-09-16-gate0-candidate.md`.

No `musca/`, `scripts/`, `tests/`, experiment result, dataset, player result or biological-model file changed in this commit.

## Local verification before commit

Authorized Windows host, Python 3.14.6:

```text
python -m unittest discover -s tests -q
Ran 75 tests
OK
```
Additional local checks before the commit:

- `git diff --check` — PASS;
- Python source compilation — PASS;
- deterministic `light_chemical` smoke — PASS, beacon reached, 6 moves, 0 collisions;
- runtime source files were unchanged.

## GitHub Actions read-back

Workflow run:

- name: `ci`;
- run number: `1`;
- run id: `35098073671`;
- event: `push`;
- head SHA: `428096325dc03bad9770266ae7328cc40f6f52d1`;
- conclusion: `success`.

Job `Windows / Python 3.14.6` completed successfully.
GitHub independently reported success for:

1. Checkout;
2. Set up Python 3.14.6;
3. Show interpreter;
4. Run unit tests;
5. Compile sources;
6. Smoke deterministic demo.

This is the first verified GitHub-hosted CI run for MUSCA.
## Scope boundary

GATE 0 verifies the current dependency-free prototype on the Windows/Python 3.14.6 reference surface. It does **not** establish Linux/macOS support, scientific validity, connectome execution, player interest, multiplayer readiness or MMO readiness.

The separate Linux/container observation remains bounded: individual test modules passed there, but the combined discovery run did not finish inside the available execution window. Therefore full cross-platform status remains `UNVERIFIED`.

The repository still has no selected project `LICENSE`; this receipt does not infer one.

## Lifecycle

`created → tested-locally → committed → pushed → CI-invoked → CI-verified`

Not claimed:

`merged-to-main`, `released`, `deployed`, `scientifically-validated`, `playtested`.

∆ — GATE 0 moved from locally tested candidate to remotely verified CI baseline.
D — commit `4280963...` plus GitHub Actions run `35098073671` and successful job-step read-back.
Ω — high for this exact commit on Windows/Python 3.14.6.
Λ — re-run verification after any new branch HEAD; cross-platform claims require their own successful CI surfaces.
