# MUSCA — SCI-R00 read-only preflight receipt

Observed: 2026-09-16.
Feature commit: `99fc611652f26781885725518cc136255e9bcffe`.

Status: `PREFLIGHT PASS / R00 BLOCKED / SCIENCE NOT RUN`.

## What was added

- `experiments/manifests/SCI-R00-SHIU-ENV-DATA.candidate.json`;
- `scripts/research_r00_preflight.py`;
- `tests/test_research_r00_preflight.py`;
- CI schema coverage for R00;
- CI assertion that current governance still blocks materialization.

The preflight is read-only by contract. Its maximum positive state is
`READY_FOR_OPERATIONAL_APPROVAL`, never `READY_TO_RUN`.

## Local Windows verification

Windows / Python 3.14.6:

- 98 unit tests PASS;
- 10 dedicated R00 preflight tests PASS;
- registered dataset/R00/R01/GATE-P01 schemas PASS;
- Python compile PASS;
- `git diff --check` PASS;
- staged secret-pattern scan PASS;
- no `musca/*.py` runtime file changed.

## Current host disposition

The actual host preflight reports two independent blockers:

1. `governance` — ADR-0005 is still `proposed`, not `accepted`;
2. `host_solver` — no `micromamba`, `mamba` or `conda` executable is present.

Additional host facts:

- `py.exe` exists, but Python 3.10 runtime is not installed (`py -3.10` exit 103);
- `uv 0.10.10` is present, but is not treated as a substitute for the pinned conda YAML environment;
- planned storage anchor `E:\` exists;
- observed free space on `E:`: `353981394944` bytes;
- `E:\MUSCA_RESEARCH` did not exist during preflight.

The preflight reported:

```text
writes_performed=0
downloads_performed=0
installs_performed=0
simulations_run=0
```

Network reachability and upstream byte retrieval were deliberately not tested.

## GitHub verification

For feature commit `99fc611...`:

- PR workflow run `35106510813` (#12): `success`;
- push workflow run `35106513906` (#13): `success`.

On both runs these steps completed successfully:

- Windows / Python 3.14.6 setup;
- unit tests;
- registered manifest schema validation;
- `Assess SCI-R00 preflight remains approval-gated`;
- compile;
- deterministic smoke demo.

## Upstream pin read-back

GitHub read-back reconfirmed upstream commit
`91bdd1e7dcf193f3e7ca5a8933497fcef63b7960` and the expected v630 tree entries.
The bundled connectivity parquet is `86630944` bytes and the completeness CSV is
`3057611` bytes. `environment_full.yml` remains the strict first environment spec.
No upstream bytes were downloaded locally by this gate.

## Claim boundary

This receipt verifies the preflight mechanism, not the neuroscience experiment.
R00 has not materialized an environment or dataset. R01 has not run. No connectome
result, topology advantage, game value or commercial-rights claim is promoted.

## Next gates

1. Project authority must explicitly accept or reject ADR-0005.
2. If accepted, choose the strict environment solver/tooling path through a
   separate operational decision/read-back.
3. Installation/download/materialization remains a separate write gate.
4. Preserve strict and compatibility reproduction lineages as different results.

∆ — R00 moved from prose-only planning to an executable, read-only approval gate.
D — commit `99fc611...`; local 98 tests; Actions #12/#13 green; zero R00 writes.
Ω — high for preflight behavior and host blockers; science remains unknown.
Λ — revise after ADR acceptance/rejection, solver installation, upstream drift or first R00 materialization.
