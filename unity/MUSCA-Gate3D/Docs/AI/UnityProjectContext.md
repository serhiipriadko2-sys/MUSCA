# MUSCA Gate3D — Unity Project Context

<!-- unity-onboarding:generated:start -->

Analyzed: 2026-09-16
Repository branch at onboarding: `feature/3d-gate-prototype`
Unity project root: `unity/MUSCA-Gate3D`

## Project summary

Fresh Unity project created specifically for the MUSCA `gate-lab-v0.2` Function prototype. It has no gameplay assets, scenes, prefabs, or first-party runtime scripts yet. The authoritative spatial reference remains `prototype3d/room-evidence/gate-lab-v0.2/` until a Unity scene is validated.

## Confirmed environment

- Unity Editor: `6000.6.1f1 (7efac9f6c10e)`
- Host: Windows 10 x64
- Render pipeline: Built-in Render Pipeline (no URP/HDRP package detected)
- Input: Legacy Input Manager (`activeInputHandler: 0`); no Input System package detected
- Color space: project default Gamma (`m_ActiveColorSpace: 0`)
- Build scenes: none

## Packages and frameworks

- Direct manifest dependencies are Unity IDE integrations plus built-in modules.
- Unity Test Framework `1.8.0` appears as a transitive dependency in `packages-lock.json`.
- No URP, HDRP, Input System, Addressables, networking, localization, DOTS, XR interaction package, or third-party runtime framework detected.
- No Unity MCP provider package or project MCP configuration detected during onboarding.

## Current structure

- `Assets/`: empty at onboarding.
- `Packages/`: generated manifest and lockfile only.
- `ProjectSettings/`: fresh Unity defaults.
- `Docs/AI/`: onboarding context only; outside Unity asset import scope.
- No `.asmdef`, `.asmref`, `.unity`, `.prefab`, or `.inputactions` files existed at onboarding.

## Architecture

No existing Unity gameplay architecture exists yet. New implementation should keep deterministic gate rules separate from MonoBehaviour presentation/input code and preserve the project-level boundary: human semantic choice -> world response; no claim that Unity visual behavior validates connectome science.

## Testing and validation

Initial Unity import completed successfully after providing process-local `PROGRAMDATA=C:\ProgramData`; this variable was absent from the Desktop Commander process environment and caused Unity Package Manager to crash before IPC startup. No system environment variable was changed.

The first createProject attempts are preserved outside the active project as diagnostic partial directories under `unity/`.

Validation for future work should include:

- Unity Editor compilation with zero new errors;
- EditMode tests for deterministic gate-state logic;
- PlayMode/runtime validation for movement, collision, interaction and HUD;
- scene/build-settings read-back after authoring;
- visual/runtime capture from the real production camera.

## Available tooling

- Unity Editor and Unity Hub are installed locally.
- Blender 5.2.0 LTS is installed; use a separate background process because an unrelated interactive Blender scene is open and unsaved.
- No callable Unity/Blender MCP tool namespace is surfaced in this Chat session. Repository/Desktop automation remains available.

## Constraints

- Frozen GATE-P01 v0.1 remains terminal-only and must not use this scene.
- `gate-lab-v0.2` is a Function prototype until explicit human Function approval.
- Do not add Unity packages without a direct need and an explicit project decision.
- Do not treat the Unity scene as neuroscience evidence.

## Unknowns

- Final engine/render-pipeline decision beyond this Function prototype: unknown.
- Controller/gamepad support: not implemented.
- Final art, lighting, sound, accessibility, localization and production performance budgets: not validated.
- Independent Unity MCP bridge: unverified outside the tools surfaced in this session.

## Sources inspected

- `ProjectSettings/ProjectVersion.txt`
- `ProjectSettings/ProjectSettings.asset`
- `ProjectSettings/EditorBuildSettings.asset`
- `Packages/manifest.json`
- `Packages/packages-lock.json`
- fresh `Assets/` tree
- Unity create/import logs
- repository MUSCA architecture and room-evidence documents

<!-- unity-onboarding:generated:end -->

## Current Gate3D v0.2 implementation — 2026-09-16

- Runtime assembly: `MUSCA.Gate3D.Runtime`; editor builder/validator: `MUSCA.Gate3D.Editor`; EditMode tests: `MUSCA.Gate3D.Tests`.
- Authoritative domain owner: plain C# `GateDomain`; MonoBehaviours bridge movement, stations, gate motion, HUD, companion and QA capture.
- Scene: `Assets/MUSCA/Scenes/GateLab.unity`, generated by `GateSceneBuilder` and enabled as the single build scene.
- Local scene validator: PASS, zero missing scripts, FOV 72°, eye 1.68 m, mirrored stations ±4.1 m.
- Unity Test Framework: 7/7 EditMode tests PASS on the current source.
- Windows Development build and standalone production-camera QA: PASS.
- Selected final evidence: `Docs/AI/RuntimeQA/pass5-spawn.png`, `pass5-amber.png`, `pass5-openedworld.png`.
- Function status: `ready_for_human_approval`; Form remains blocked pending explicit approval.
- No Unity MCP provider is installed/detected in project configuration in this chat surface; batch Editor automation is the verified control path.
- Known environment warning: Unity external TLS checks emit `Curl error 35` on shutdown; local UPM/import/build/runtime remain operational.
